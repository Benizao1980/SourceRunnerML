# Salmonella pilot results

No performance results are claimed yet.

When the pilot is run, summarize only compact, reviewer-friendly outputs here:

- dataset/source counts after QC;
- model-comparison metrics;
- confusion matrix;
- class-specific performance;
- probability calibration / uncertainty summary where available;
- one or two example figures;
- a short limitations section.

Large result directories, fitted model objects and full prediction tables should remain outside the repository unless there is a clear reason to publish them.
